<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Edit Absence</title>
    <style>
      /* Body styles */
      body {
        font-family: Arial, sans-serif;
        background-color: #FFFFFF;
        color: #03256C;
        margin: 0;
        padding: 0;
      }

      /* Heading styles */
      h1 {
        color: #2541B2;
        font-size: 2.5rem;
        text-align: center;
        margin-top: 2rem;
        margin-bottom: 2rem;
      }

      /* Form styles */
      form {
        width: 70%;
        margin: auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        background-color: #FFFFFF;
        border-radius: 1rem;
        padding: 2rem;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
      }

      form label {
        font-size: 1.2rem;
        margin-top: 1rem;
      }

      form input[type="date"],
      form input[type="text"] {
        padding: 0.5rem;
        margin-top: 0.5rem;
        margin-bottom: 1rem;
        border: none;
        border-bottom: 2px solid #2541B2;
        width: 100%;
        font-size: 1.2rem;
      }

      form input[type="submit"] {
        background-color: #2541B2;
        color: #FFFFFF;
        padding: 0.8rem 1.2rem;
        border: none;
        border-radius: 0.5rem;
        font-size: 1.2rem;
        cursor: pointer;
        margin-top: 1rem;
        transition: background-color 0.3s ease-in-out;
      }

      form input[type="submit"]:hover {
        background-color: #1768AC;
      }
    </style>
  </head>
  <body>
    <h1>Edit Absence</h1>
    <form action="/absences/<%= absence.id %>/editAbsence" method="POST">
      <label for="start_date">Start Date:</label>
      <input type="date" id="start_date" name="start_date" value="<%= absence.start_date %>">
      <label for="end_date">End Date:</label>
      <input type="date" id="end_date" name="end_date" value="<%= absence.end_date %>">
      <label for="reason">Reason:</label>
      <input type="text" id="reason" name="reason" value="<%= absence.reason %>">
      <input type="submit" value="Update Absence">
    </form>
  </body>
</html>

